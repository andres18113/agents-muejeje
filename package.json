{
  "name": "claude-agents-mcp",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Local MCP server that exposes profile-driven fresh Claude Code specialists to Codex CLI.",
  "scripts": {
    "start": "node src/index.mjs",
    "test": "node --test tests/agent-registry.test.mjs tests/delegate-agent.test.mjs",
    "check": "node --check src/index.mjs && node --check src/agent-registry.mjs && node --check src/agent-contracts.mjs && node --check src/delegate-agent.mjs && node --check src/prompt-composer.mjs && node --check src/claude-runner.mjs",
    "ci": "npm run check && npm test"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.5.4"
  }
}
